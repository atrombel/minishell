#include "minishell.h"
#include "atrombel.h"

void	multiple_cmd(t_list *cmd_head, t_data *data, t_env **env)
{
	(void)cmd_head;
	(void)data;
	(void)env;
	printf("work in progress come back later\n");
}
